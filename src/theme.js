// theme.js

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#00a2be", // Customize the primary color here
    },
    secondary: {
      main: "#d42559", // Customize the secondary color here
    },
  },
});

export default theme;
